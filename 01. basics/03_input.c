#include <stdio.h>

int main() {
    int n;

    printf("Enter a num: ");  // Display prompt separately
    scanf("%d", &n);          // Correct format for input

    printf("You entered: %d\n", n);
    return 0;
}
